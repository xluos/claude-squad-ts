{
  "name": "claude-squad-ts",
  "version": "0.2.0",
  "description": "TypeScript rewrite of claude-squad — manage multiple Claude/Aider sessions over git worktrees + tmux. Built on SolidJS + OpenTUI for native IME / CJK support.",
  "type": "module",
  "bin": {
    "cs": "./dist/cli/index.js",
    "claude-squad-ts": "./dist/cli/index.js"
  },
  "main": "./dist/cli/index.js",
  "scripts": {
    "dev": "bun run src/cli/index.ts",
    "dev:tui": "bun run src/cli/index.ts",
    "build": "bun build src/cli/index.ts --target=bun --outdir=dist --minify --sourcemap",
    "start": "bun run dist/cli/index.js",
    "check": "tsc --noEmit && biome check src",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src",
    "lint:fix": "biome check --write src",
    "format": "biome format --write src",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "engines": {
    "bun": ">=1.2",
    "node": ">=20"
  },
  "dependencies": {
    "@lydell/node-pty": "1.2.0-beta.12",
    "@opentui/core": "0.2.14",
    "@opentui/keymap": "0.2.14",
    "@opentui/solid": "0.2.14",
    "commander": "^14.0.3",
    "simple-git": "^3.36.0",
    "solid-js": "1.9.12"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.0",
    "@types/bun": "latest",
    "typescript": "^5.9.0"
  },
  "trustedDependencies": [
    "@lydell/node-pty",
    "@lydell/node-pty-darwin-arm64",
    "@lydell/node-pty-darwin-x64",
    "@lydell/node-pty-linux-x64",
    "@lydell/node-pty-linux-arm64",
    "@opentui/core",
    "@opentui/core-darwin-arm64",
    "@opentui/core-darwin-x64",
    "@opentui/core-linux-arm64",
    "@opentui/core-linux-x64"
  ],
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ]
}
